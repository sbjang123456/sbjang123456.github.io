---
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '@site/ui/components/card';
import { type Career, formatPeriod } from '../data';

interface Props {
  careers: Career[];
}

const { careers } = Astro.props;
---

<Card>
  <CardHeader>
    <CardTitle>경력</CardTitle>
    <CardDescription>조직과 기간, 맡은 일</CardDescription>
  </CardHeader>
  <CardContent>
    {
      careers.length === 0 ? (
        <p class="text-sm text-muted-foreground">(작성 예정)</p>
      ) : (
        <ol class="flex flex-col gap-8">
          {careers.map((career) => (
            <li class="flex flex-col gap-2">
              <div class="flex flex-wrap items-baseline justify-between gap-x-3">
                <h3 class="font-medium">
                  {career.org}
                  <span class="text-muted-foreground"> · {career.role}</span>
                </h3>
                <span class="text-sm text-muted-foreground tabular-nums">
                  {formatPeriod(career)}
                </span>
              </div>
              {career.highlights.length > 0 && (
                <ul class="flex list-disc flex-col gap-1 pl-5 text-sm text-muted-foreground">
                  {career.highlights.map((highlight) => (
                    <li>{highlight}</li>
                  ))}
                </ul>
              )}
              {career.projects.length > 0 && (
                /* 프로젝트 이름은 한 줄에 안 들어갈 만큼 길다 — 배지(nowrap)로
                   깔면 좁은 화면에서 가로 스크롤이 생긴다. 목록으로 흘린다. */
                <div
                  data-slot="projects"
                  class="mt-1 flex flex-col gap-1 border-l-2 border-border pl-3"
                >
                  <h4 class="text-xs font-medium">프로젝트</h4>
                  <ul class="flex flex-col gap-0.5 text-xs text-muted-foreground">
                    {career.projects.map((project) => (
                      <li>{project}</li>
                    ))}
                  </ul>
                </div>
              )}
            </li>
          ))}
        </ol>
      )
    }
  </CardContent>
</Card>
